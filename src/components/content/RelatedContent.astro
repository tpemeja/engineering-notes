---
import type { CollectionEntry } from 'astro:content';
import ProjectCard from './ProjectCard.astro';
import NoteCard from './NoteCard.astro';
import JournalEntryCard from './JournalEntryCard.astro';

interface Props {
  title?: string;
  projects?: CollectionEntry<'projects'>[];
  notes?: CollectionEntry<'notes'>[];
  journal?: CollectionEntry<'journal'>[];
}

const { title = 'Related', projects = [], notes = [], journal = [] } = Astro.props;
const hasContent = projects.length > 0 || notes.length > 0 || journal.length > 0;
---

{
  hasContent && (
    <section class="related-content not-prose" aria-label={title}>
      <h2>{title}</h2>
      <div class="related-grid">
        {projects.map((project) => (
          <ProjectCard project={project} />
        ))}
        {notes.map((note) => (
          <NoteCard note={note} />
        ))}
        {journal.map((entry) => (
          <JournalEntryCard entry={entry} />
        ))}
      </div>
    </section>
  )
}

<style>
  .related-content {
    margin-block-start: 2.5rem;
    padding-block-start: 2rem;
    border-top: 1px solid var(--color-border);
  }

  .related-content h2 {
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    color: var(--color-fg-muted);
    margin: 0 0 1rem 0;
  }

  .related-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 1rem;
  }
</style>
