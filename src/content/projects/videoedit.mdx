---
title: 'VideoEdit'
description: 'Local web app that turns a CapCut cooking-video export into bilingual captions and dialogue transcripts, either as SRTs or written straight back into the CapCut draft.'
status: active
startDate: 2026-07-11
technologies:
  [
    'Python',
    'React',
    'faster-whisper',
    'Gemini',
  ]
technologyNotes:
  - name: 'Python'
    context: 'Backend language for the pipeline and AI calls.'
  - name: 'TypeScript'
    context: 'Typed frontend, sharing shapes with the backend models.'
  - name: 'FastAPI'
    context: 'Serves the local API the React frontend talks to.'
  - name: 'React'
    context: 'Powers the local web UI, built with Vite.'
  - name: 'faster-whisper'
    context: 'Per-language transcription passes feeding the Gemini merge.'
  - name: 'Gemini'
    context: 'Generates captions, merges transcripts, and translates.'
  - name: 'PySceneDetect'
    context: 'Splits the raw export into scenes via content detection.'
  - name: 'ffmpeg'
    context: 'Extracts audio clips and measures/renders LUFS loudness.'
phases:
  - id: 'core-pipeline'
    status: 'done'
    summary: 'Upload → scenedetect → filmstrip → merge/mark sections → dish
      context → Gemini captions → hybrid whisper/Gemini dialogue
      transcription → SRT export, the full v1 slice.'
  - id: 'capcut-direct-write'
    status: 'done'
    summary: 'Writes styled caption tracks straight into the linked CapCut
      draft instead of an SRT re-import, reverse-engineered against a real
      CapCut install: two JSON files to keep in sync, a hard close-before-
      write requirement, and a group_id sweep instead of remembered ids.'
  - id: 'profiles-multilingual'
    status: 'done'
    summary: 'Named per-project presets (language pair, CapCut style,
      per-prompt overrides) replacing a single global settings singleton,
      migrated automatically for the existing install.'
  - id: 'audio-levels'
    status: 'done'
    summary: 'Balances inconsistent voiceover volume across CapCut segments
      via measured LUFS and a peak-safe gain written into the draft, with
      spike detection flagging clips a flat gain cannot safely fix.'
  - id: 'capcut-position-calibration'
    status: 'done'
    summary: 'Solves CapCut boxes carrying no positional signal for their
      own wrap-driven growth by bucketing literal positions from a
      reference draft, plus user-confirmed wrap examples to correct
      font-metrics simulation drift.'
  - id: 'bulk-csv-editing'
    status: 'done'
    summary: 'Round-trippable CSV export/import per section-kind, with a
      row-id scheme that doubles as a caption splitter/merger and a
      delimiter sniffer to survive an Excel re-save.'
  - id: 'caption-chat-history'
    status: 'done'
    summary: 'Replaced the one-shot "generate all captions" button with a
      persistent global caption chat and a section lock, backed by a
      caption/transcript history system: per-line snapshots keyed by time
      range, whole-section snapshots across split/unsplit changes, a
      self-healing dedup pass, and word-level diff highlighting on every
      history preview.'
  - id: 'caption-examples-hook'
    status: 'done'
    summary: 'Added real-caption few-shot examples pulled from picked
      CapCut drafts, individually flagged for recipe-step vs. hook use,
      plus a new "hook" section kind for the video''s opening line that
      reuses the cooking-section editor and chat UI end to end.'
  - id: 'capcut-config-check'
    status: 'done'
    summary: 'Read-only step flagging captions in the linked CapCut draft
      whose position and style disagree about whether they''re primary or
      secondary, built after a caption copy-pasted into the wrong track''s
      slot and never restyled.'
relatedNotesSlugs: []
order: 10
progress: 90
---

import Callout from '../../components/content/Callout.astro';
import PhaseCarousel from '../../components/content/PhaseCarousel.astro';
import TechStack from '../../components/content/TechStack.astro';
import ObjectDiagram from '../../components/content/ObjectDiagram.astro';
import DecisionTable from '../../components/content/DecisionTable.astro';
import BranchDiagram from '../../components/content/BranchDiagram.astro';
import Screenshot from '../../components/content/Screenshot.astro';

## Overview

A local web app for subtitling my wife's TikTok/Instagram cooking videos.
She edits in CapCut desktop and exports an mp4. This tool splits that
export into scenes, groups them into logical sections, proposes short
recipe-step captions with Gemini, transcribes the tasting/dialogue moments,
and hands the result back either as SRT files to import into CapCut, or
written directly into the linked CapCut draft so the styling and
positioning she's already dialed in for the primary/secondary caption
tracks carry over automatically.

Most of the actual engineering weight sits in `capcut_captions.py` and its 
neighbors. CapCut's draft format is undocumented, versioned across app 
releases, and cached in memory while the app is open. Getting captions 
and audio levels written into it correctly took real reverse-engineering 
against a live install, not just following a spec.

## Problem

- **Consistent bilingual captions, without redoing styling by
  hand every video.** She needs a short, imperative recipe-step caption
  style, plus a verbatim-and-translated dialogue track for tasting
  moments, in whatever language pair a given video needs. Doing that by
  hand per video is repetitive and easy to make inconsistent.
- **CapCut's draft format has no public spec.** Writing captions and audio
  gain directly into a `draft_info.json` means reverse-engineering
  undocumented, versioned, partially-duplicated state (see CapCut
  direct-write) against a real install, not implementing a documented API.
- **Voiceover volume varies clip to clip.** Fixing that by ear inside
  CapCut is repetitive and easy to get subtly wrong. A single loud
  transient in an otherwise-quiet clip can make a naive fix clip audio that
  should have stayed clean (see Balance Audio Levels).

None of these is exotic alone, but together they're why this is a real
tool rather than a five-minute Whisper script: a genuinely useful output
needs consistent styling and positioning that survives all the way into
the editor she actually works in, not just a technically-correct SRT file.

## Phases

Nine phases so far, from the first working pipeline slice to a read-only
check that catches a CapCut draft edited by hand out from under the app:

<PhaseCarousel phases={frontmatter.phases}>

### Core pipeline |#core-pipeline|

The v1 slice end to end: upload the CapCut export, `PySceneDetect`
(content detection, threshold ~40) splits it into scenes, adjacent scenes
get merged into logical **sections** in the UI, sections containing speech
are marked **dialogue**, dish context (name/ingredients/steps/description)
is entered once per video, Gemini proposes captions for every cooking
section in one coherent pass, dialogue sections get transcribed, and
everything is editable in the UI before exporting SRTs.

<ObjectDiagram
  arrowLabel="merged into"
  nodes={[
    {
      name: 'Scene',
      fields: [
        { name: 'index / start / end', detail: 'one scenedetect cut' },
        { name: 'keyframe', detail: 'middle-frame jpg, saved per scene' },
      ],
    },
    {
      name: 'Section',
      fields: [
        { name: 'kind', detail: 'cooking / dialogue / custom' },
        { name: 'scene_start / scene_end', detail: 'the scene range it spans' },
        { name: 'captions', detail: 'dict[lang, text], the whole-section caption' },
        { name: 'caption_lines / transcript_lines', detail: 'optional split into timed sub-lines' },
      ],
    },
  ]}
/>

<Screenshot
  src="/images/projects/videoedit/project-list.png"
  alt="VideoEdit project list showing four projects, each with a scene/section count, plus the upload form for a new one"
  caption="Every project is just a folder scanned off disk — no database. The upload form on top starts a new one from a CapCut export."
/>

<Screenshot
  src="/images/projects/videoedit/workspace-captions.png"
  alt="VideoEdit project workspace with the filmstrip of four detected scenes, dish context inputs, and a fully generated FR/EN caption table"
  caption="The per-project workspace: scenedetect's scenes in the filmstrip, optional dish context above the captions, and Gemini's generated FR/EN captions per section below."
/>

Dialogue sections get a hybrid transcription rather than a single Gemini or
whisper pass: three forced-language `faster-whisper` passes for word-level
timestamps (fixed at en/fr/ja regardless of the profile's own output
languages, purely to help Gemini tell code-switched speech apart), then
Gemini reads the audio plus all three passes and produces one merged,
code-switched transcript aligned to the whisper timings:

<BranchDiagram
  source={{
    name: "Dialogue section's audio clip",
    detail: 'extracted via ffmpeg, 16kHz mono',
  }}
  branchLabel="3 forced-language faster-whisper passes"
  branches={[
    { name: 'en pass', detail: 'word-level timestamps, forced to English' },
    { name: 'fr pass', detail: 'word-level timestamps, forced to French' },
    { name: 'ja pass', detail: 'word-level timestamps, forced to Japanese' },
  ]}
  mergeLabel="Gemini reads the audio + all 3 passes"
  merge={{
    name: 'Merged transcript',
    detail: 'one chronological, code-switch-resolved transcript, timed against whisper',
  }}
/>

<DecisionTable
  decisions={[
    {
      label: 'Input (v1)',
      chose: 'Exported mp4 + scenedetect',
      insteadOf: "Parsing CapCut's own draft_content.json for an exact cut list",
      why: 'Works without touching CapCut internals at all for the input side; exact-cut-list parsing (no detection errors) is deferred to v2.',
    },
    {
      label: 'Caption granularity',
      chose: 'Attach to merged sections, not raw scenes',
      insteadOf: 'One caption per scenedetect scene',
      why: "A section is the editorially meaningful unit (one recipe step can span several scene cuts); captioning raw scenes would fragment a single idea across several short captions.",
    },
    {
      label: 'Persistence',
      chose: 'A projects/ folder per video (mp4, keyframes/, project.json)',
      insteadOf: 'A database',
      why: 'The app just lists projects by scanning the folder, and a project is trivially copyable and inspectable as plain files.',
    },
  ]}
/>

<Callout variant="lesson" title="A local single-user app still needs to survive concurrent writes to itself">
  `project.json` gets written by whichever request finishes last: an
  autosave from an edit, a caption regeneration, or a CapCut write. An
  in-place overwrite can leave a half-written file if two of those land
  close together. Every write goes through a tempfile-then-`os.replace`
  pattern instead (write to a temp file in the same directory, then
  atomically swap it in), so a reader never sees a partial file, even
  without any locking. The same pattern got reused later for
  `profiles.json` and `settings.json`.
</Callout>

### CapCut direct-write |#capcut-direct-write|

The bigger payoff over plain SRT export: `write_captions()`
(`capcut_captions.py`) writes styled primary (top, black-on-white
per-line highlight) and secondary (bottom, white-with-shadow) text tracks
straight into the CapCut draft linked to a project, so an SRT re-import and
manual restyling per video isn't needed at all. None of this is
documented anywhere. Every rule below came from inspecting a real
`CAPCUT_PC/9.1.0-beta4` draft on disk and testing against a live install.

<Callout variant="warning" title="CapCut caches its draft in memory: an external edit while it's open is invisible or gets clobbered">
  CapCut loads a draft into memory on open and has no mechanism to pick up
  external file changes. An edit made while it's open either doesn't show,
  or gets silently overwritten when CapCut's own in-memory state flushes
  back to disk on close. `is_draft_locked()` checks for a `.locked` file in
  the draft folder and blocks writes while a project is open. That's a
  hard technical limit this tool has to respect, not just a safety gate,
  confirmed against other independent CapCut-editing tools, none of which
  claim a hot-reload path either.
</Callout>

<Callout variant="warning" title="Newer CapCut versions store the draft in two places, and both need writing">
  The legacy top-level `draft_info.json`, and the actual render source,
  `Timelines/<timeline_id>/draft_info.json`. CapCut resyncs the top-level
  file from the nested one on open/close, so writing only the top-level
  file looks correct on disk but silently never renders.
</Callout>

<DecisionTable
  decisions={[
    {
      label: 'Replace-on-rewrite',
      chose: 'Sweep every text material whose group_id starts with videoedit_',
      insteadOf: 'Deleting only the ids remembered from the last write',
      why: 'Self-healing even if the remembered-ids state file desyncs from the draft, for example after a backend --reload restart mid-write. An earlier version that trusted only remembered ids left orphaned duplicate caption tracks stacked in the draft.',
    },
    {
      label: 'Backups',
      chose: 'Back up both target files before every write',
      insteadOf: 'Trusting the write to be safe',
      why: "A caption write mutates a real, hand-styled draft in place. A bad write with no backup would be destructive to work that can't be regenerated.",
    },
    {
      label: 'History model',
      chose: 'One shared restore timeline for captions and loudness writes',
      insteadOf: 'A separate history per feature',
      why: "draft_info.json is one file per location, so restoring any entry always reverts the whole file. Separate per-feature histories would misrepresent what a restore actually does (see Balance Audio Levels).",
    },
  ]}
/>

### Profiles & multilingual |#profiles-multilingual|

Originally a single global settings singleton (one style source, one set of
prompts, a fixed fr-primary/en-secondary pair). **Profiles** replace that
with a named preset picked once per project at creation time, so the same
install can cover more than one language pair, style, or prompt setup.
Useful for a second show, or just a different caption tone.

<ObjectDiagram
  nodes={[
    {
      name: 'Profile',
      fields: [
        { name: 'primary_language (+ optional secondary)', detail: 'from en/fr/ja, scopes captions, transcripts, and SRT export' },
        { name: 'capcut_style_source_id + cached styles/positions', detail: 'a CapCut draft picked once to copy caption styling and positioning from' },
        { name: 'caption/custom-text/translate prompt overrides', detail: 'plus *_send_keyframes / *_send_dish_context toggles' },
      ],
    },
  ]}
/>

<DecisionTable
  decisions={[
    {
      label: 'Caption storage',
      chose: 'dict[lang_code, text] on every caption/transcript line',
      insteadOf: 'Fixed en/fr/ja fields',
      why: "Naturally holds whatever subset of languages a project's profile calls for, instead of leaving unused fixed fields blank per project.",
    },
    {
      label: 'Migration',
      chose: 'Auto-fold the old singleton settings into a "Default" profile the first time profiles.json is needed',
      insteadOf: 'A manual one-time setup step',
      why: "The existing install, mine and my wife's, already had projects depending on the old fr/en behavior. _migrate_from_settings preserves it as a Default profile automatically, so nobody has to notice or re-configure anything.",
    },
  ]}
/>

<Callout variant="decision" title="settings.json keeps only the machine-wide API key">
  Everything that varies per workflow (style, prompts, toggles, language
  pair) moved into `profiles.json`; `settings.json` was stripped down to
  just the Gemini API key override, since that's the one thing that's
  genuinely global to a machine rather than to a project or a person's
  preferences.
</Callout>

### Balance Audio Levels |#audio-levels|

Voiceover clips can come out louder or quieter than their neighbors
depending on how close to the mic a given take was. `capcut_audio.py`'s
`analyze()`/`apply()` fix that directly inside the linked CapCut draft: measure
each eligible segment's integrated loudness, push it toward a target, write
the gain into the segment's existing `volume` field. No re-encoding, no
touching the original media.

<BranchDiagram
  source={{
    name: "Segment's loudnorm analysis pass",
    detail: 'ffmpeg, on the exact source_timerange slice actually used',
  }}
  branchLabel="wanted gain = target_lufs − measured_lufs, capped so true peak never exceeds −1 dBTP"
  branches={[
    { name: 'Cap eats ≤3dB of wanted gain', detail: 'written directly into volume, for most clips' },
    { name: 'Cap eats >3dB of wanted gain', detail: 'a brief spike (e.g. a mic bump) is blocking most of the real gain, so it gets flagged and excluded from apply() instead of silently under-normalized' },
  ]}
  mergeLabel="only non-flagged, non-zero-volume segments get written"
  merge={{
    name: 'New volume field',
    detail: 'linear gain multiplier, original media untouched',
  }}
/>

<Callout variant="lesson" title="A brief peak can silently starve an otherwise-quiet clip of its needed gain">
  A single loud transient (a mic bump) can push a segment's true peak right
  up to the safety ceiling even though the rest of the clip, the actual
  speech, is quiet. The peak cap then blocks nearly all of the wanted
  gain, leaving that one clip inconsistent with its properly-boosted
  neighbors, found via dogfooding on a clip with exactly this problem.
  Flagging and excluding it from `apply()` entirely (rather than writing
  its near-zero capped gain) surfaces it for manual handling instead of
  quietly under-normalizing it. Fixing it automatically would need
  per-sample dynamic processing (a limiter), out of scope for a flat-gain,
  no-re-encoding design.
</Callout>

Flagged clips stay selectable, just default-unchecked (normal clips
default-checked). Checking one is an explicit override: it writes the
*uncapped* wanted gain instead of the near-useless capped one. Otherwise
"selecting" a flagged clip would do nothing audible. Each clip gets an
inline before/after audio preview (one `<audio>` element with a tab toggle
swapping its `src`, rather than two full-width players, which ate too much
space in a long clip list) so the fix can be heard before it's committed.

### CapCut position & wrap calibration |#capcut-position-calibration|

CapCut lets a wrapped caption's extra lines overflow past its declared
single-line box without moving the box's own anchor. That means there's
no anchor-position signal to derive a wrapped caption's true per-line
growth from. Getting primary/secondary caption pairs to sit correctly regardless
of how many lines either one wraps to needed a different source of truth
than the box's own reported position.

<ObjectDiagram
  arrowLabel="bucketed by which track(s) wrap"
  nodes={[
    {
      name: 'Reference draft',
      fields: [
        { name: 'every primary/secondary caption pair', detail: 'matched by shared start time' },
      ],
    },
    {
      name: 'CapCutCaptionPositions',
      fields: [
        { name: 'neither', detail: 'both tracks single-line' },
        { name: 'primary_only', detail: 'only the primary track wraps' },
        { name: 'secondary_only', detail: 'only the secondary track wraps' },
        { name: 'both', detail: 'both tracks wrap' },
      ],
    },
  ]}
/>

A caption being written picks its bucket's literal `transform_x`/`transform_y`
when the reference draft had a real example of it, and falls back to a
formula (primary at its base position, secondary one gap below plus a
tunable extra-gap fraction per extra line) only for a bucket the reference
draft never actually demonstrated.

<Callout variant="lesson" title="Simulating a third-party renderer's text layout only gets you so close">
  Wrap detection simulates CapCut's own Montserrat-Bold rendering against
  the style's captured box width to decide whether a caption will wrap.
  But even with the real captured `line_max_width` and correct font
  metrics, it still missed a real wrap that CapCut itself produced, with
  no other field to explain the gap. `wrap_examples` (user-confirmed
  wraps/non-wraps observed directly in a real draft) override the
  simulated threshold instead: any confirmed wrap narrows the threshold to
  that example's width, since missing a real wrap is worse than
  over-detecting one, even if it conflicts with a confirmed non-wrap.
</Callout>

<DecisionTable
  decisions={[
    {
      label: 'Calibration bootstrap',
      chose: 'A throwaway "calibration project": 4 concatenated 1s flat-color clips, captions forced to each of the 4 wrap combinations via explicit \\n',
      insteadOf: "Relying on a real video's natural line breaks to happen to cover all 4 buckets",
      why: "Explicit \\n forces exactly the 4 wrap combinations by construction, canvas-size-independent, instead of hoping a real project's captions happen to wrap every way.",
    },
    {
      label: 'Character-level fallback',
      chose: 'Approximate character-by-character wrapping for a single space-less token wider than the box',
      insteadOf: 'Space-splitting only',
      why: "A long URL, or this tool's own calibration fixtures, can't be broken by space-wrapping at all. Plain space-splitting can't catch that on its own.",
    },
  ]}
/>

One manual step remains by design: this tool can't drive CapCut itself, so
"Create calibration project" still requires importing the placeholder
video into CapCut once, dragging each of the 4 sections' boxes (and style,
since the same draft doubles as the style source) to taste, then picking
that draft as the profile's CapCut style source as usual.

### Bulk CSV editing |#bulk-csv-editing|

Editing captions one field at a time in the UI is fine for a few
corrections, but slow for a full-video pass or for handing captions to
someone else (a spreadsheet, ChatGPT) to edit in bulk. `captions_bulk.py`
exports/imports one CSV per section-kind (cooking/dialogue/custom), scoped
to the editor card it came from.

<DecisionTable
  decisions={[
    {
      label: 'Row identity',
      chose: 'scene_start, or scene_start-line_number for a split section',
      insteadOf: "The app's own internal section id",
      why: 'A legible, stable id (a section id accretes an "-a"/"-b" suffix per merge/split and can get long); the line-number suffix disambiguates a section split into several sequential lines, which otherwise all share the same scene_start.',
    },
    {
      label: 'Rebuild strategy',
      chose: "Rebuild a scene's lines wholly from its CSV row group",
      insteadOf: 'Patching lines index-by-index',
      why: 'Doubles as a splitter/merger for free. Giving a scene more or fewer rows than it had lines just changes the resulting split, with no separate split/merge codepath to maintain.',
    },
    {
      label: 'Bad rows',
      chose: 'Skip and report, not fail the whole import',
      insteadOf: 'Rejecting the file on any invalid row',
      why: 'A partially-stale export (sections changed since it was downloaded) should still apply everything it validly can, not block on the parts that no longer match.',
    },
  ]}
/>

<Callout variant="lesson" title="A CSV round-trip through a spreadsheet can silently change its own delimiter">
  Export always writes comma-separated, but Excel re-saves a CSV using
  whatever the system locale's list separator is (a semicolon for French,
  for instance). Assuming comma on import would silently misparse a re-saved
  file. `csv.Sniffer` detects the actual delimiter used (falling back to
  comma), so the round trip survives a spreadsheet in the middle
  regardless of locale.
</Callout>

### Caption chat & history |#caption-chat-history|

The original "generate captions for all sections" button was one-shot:
call it, captions land immediately, no memory of what was just proposed.
A bad batch was expensive to walk back and impossible to steer
mid-generation. It's replaced by a **global caption chat**, a persistent,
project-wide conversation that proposes captions for every non-locked
cooking section per turn and never auto-applies, plus a general
caption/transcript **history** system that every kind of write, manual
edit, chat apply, translate, or CSV import, now feeds into.

<ObjectDiagram
  arrowLabel="every write snapshots into"
  nodes={[
    {
      name: 'Section',
      fields: [
        { name: 'captions_locked', detail: 'excludes this section from future global-chat turns' },
        { name: 'caption_history', detail: 'per-(start, end) snapshots, oldest dropped past 5' },
        { name: 'section_history', detail: 'whole-section snapshots spanning a split/unsplit change' },
      ],
    },
    {
      name: 'CaptionHistoryEntry / CaptionSectionSnapshot',
      fields: [
        { name: 'source', detail: 'manual / chat / global-chat / translate / csv / revert' },
        { name: 'timestamp', detail: 'shared across a whole batch apply, for grouping' },
      ],
    },
  ]}
/>

Proposals from the global chat render as a batch review list, checkbox
per section, current text against proposed, with one "Apply N selected"
button, mirroring the Balance Audio Levels preview/apply pattern instead
of per-section "Use this" buttons. Applied state tracks per section
rather than for the whole batch, so undoing one applied section, or
adding one you'd originally skipped, doesn't mean redoing the rest.

<DecisionTable
  decisions={[
    {
      label: 'History key',
      chose: "A line's own (start, end) time range",
      insteadOf: 'Array index or a stored id',
      why: 'Robust to sibling lines being inserted, reordered, or removed around it, so a single split caption line stays independently revertible from its neighbors.',
    },
    {
      label: 'Sequential subtitles',
      chose: 'Whole-section snapshot only, no per-line history',
      insteadOf: 'Per-line history with a line_index fallback for retimes',
      why: "Retiming a split line (drag, \"space evenly\") breaks the (start, end) key that per-line history depends on. A line_index fallback worked, but the simpler fix asked for was dropping per-line history for split content entirely.",
    },
    {
      label: 'Dedup guard',
      chose: 'Skip appending when the text matches any existing entry for that key',
      insteadOf: 'Only checking the immediately-preceding entry',
      why: 'Manual edits autosave on every keystroke, and retyping back to an earlier value is common. Checking only the previous entry still let real duplicates accumulate at non-adjacent positions.',
    },
  ]}
/>

<Callout variant="lesson" title="A dedup guard only stops new duplicates, not ones already saved">
  Fixing the dedup guard stopped new duplicate entries but did nothing
  for history lists that already had them, some going back to before the
  guard existed. A self-healing pass runs on every section on every
  project load and collapses each history list down to one occurrence per
  distinct value, keeping the most recent. Confirmed directly against a
  real project: 4 duplicate entries for one caption collapsed to 1 the
  next time it was opened, no edit required.
</Callout>

Reverting a snapshot now records the value it's about to overwrite
first, under a dedicated "revert" source, so undoing a revert is just
reverting again instead of having nothing to go back to. Every history
preview, the per-line popover, the section history panel, the
global-chat review list, renders as a word-level diff against the
section's current live value rather than plain text, so a preview
answers "what would actually change right now," not just "what did this
used to say."

### Caption examples & video hook |#caption-examples-hook|

Two related caption-quality additions: real-caption few-shot examples
pulled from past CapCut projects, and a dedicated "hook" section kind for
the video's opening line.

**Example projects** (`Profile.example_projects`): pick CapCut drafts in
Settings whose captions are already good, and every one of their real
captions gets extracted, reusing the same top-two-text-tracks-by-position
heuristic as style extraction, as a candidate few-shot example,
individually flagged for whether it's a recipe-step example, a hook
example, both, or neither. Gemini gets whichever ones are flagged on as
real past outputs to imitate the voice of, kept separate from the style
prompt the same way per-language grammar notes already were.

<Callout variant="decision" title="A time cutoff seeds the flags, but the flags are the real mechanism">
  Extraction defaults `use_for_hook` on for whatever falls before a
  configurable cutoff, and `use_for_captions` on for everything, but a
  time cutoff alone can't reliably tell which lines were actually the
  hook. Settings shows every extracted caption as a checkbox pair
  instead, so each one can be reviewed and corrected. An earlier version
  auto-split by cutoff with no override; the ask that replaced it was to
  see the real subtitles and choose.
</Callout>

**Hook** is a fourth `SectionKind` alongside cooking/dialogue/custom: the
video's opening line, attention-grabbing rather than an instruction,
marked the same click-to-cycle way as the other three, with no
cardinality enforcement. It reuses the exact caption editor and chat UI
cooking sections use, `CaptionEditor` gained a `kind` prop rather than a
separate `HookEditor` component, since the two are otherwise identical:
same history buttons, same translate, same split-into-lines. A hook can
still be a short sequence (a question, then a reveal), so
`chat_about_hook` keeps the same single-or-split proposal schema as the
per-section cooking chat, just with hook-specific wording and its own
style prompt and keyframe/dish-context toggles. Hook sections are
automatically excluded from the global caption chat, which already
hard-filters to cooking sections, and get their own step between
Captions and Transcripts.

### CapCut configuration check |#capcut-config-check|

A read-only step that flags captions already written into the linked
CapCut draft whose position and style disagree about which role,
primary or secondary, they actually belong to. Built after a real
incident: a caption copy-pasted from the primary track into the
secondary track's slot, text edited, but never restyled to match.

The check reads every text segment currently in the draft's
`draft_info.json` fresh off disk, not just what the app itself wrote,
since the failure mode it's built for happens through edits made
directly in CapCut, outside the app's own bookkeeping. Each segment gets
classified two ways, independently: by *position role* (its transform
closer to the primary or secondary preset's own anchor, a coarser check
than the real per-wrap-bucket positions the direct-write path uses,
since telling primary from secondary apart doesn't need bucket-level
precision) and by *style role* (exact-field match count against both
presets, with fields that legitimately vary per wrap bucket or that
CapCut sets to its own factory defaults excluded, so they don't dilute
the match).

<Callout variant="decision" title="Issues only, no fix action">
  The check surfaces a list of disagreements and nothing else: no
  pass/fail recap, no one-click fix. A mismatch might be an intentional
  one-off, and silently overwriting it on the app's own judgment could
  destroy a deliberate customization. Read-only by design.
</Callout>

</PhaseCarousel>

## Tech stack

What each piece of the stack is actually doing, and where to look for more:

<TechStack notes={frontmatter.technologyNotes} />

## Approach

Every CapCut-facing feature (direct-write, audio levels, position
calibration, configuration check) was built and verified against a real
CapCut install rather than against assumptions about the format, since
none of it is documented. That discipline caught concrete, otherwise-
invisible failures: captions that looked written but never rendered (the
untouched nested Timelines copy), orphaned duplicate tracks from a
desynced remembered-ids state file, a spike-flattened clip that a flat
gain formula would have silently left inconsistent with its neighbors,
and a caption edited by hand in CapCut that kept its old track's styling,
which the configuration check exists specifically to catch. The same
build-against-real-usage discipline caught a quietly-accumulating
duplicate-history bug in the caption history system, only visible once
looked for directly in a real project's saved data.

## Future improvements

Straight from the project's own deferred-to-v2 list:

- **CapCut draft JSON as input**, not just output. Parsing
  `draft_content.json` directly for an exact cut list with no scenedetect
  errors, complementing the direct-write already shipped on the output
  side.
- **A caption style picker per video** (clean / quantities / playful),
  instead of one style per profile.
- **Per-section regenerate with a corrective note**, instead of
  regenerating a whole video's captions at once.
- **Multi-account support with authentication.** Today's profiles are
  unauthenticated presets within one local install, not logged-in
  identities. Deliberately out of scope while this stays a two-person
  tool.
