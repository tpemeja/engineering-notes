---
title: '[Project Name]'
description: '[One-sentence summary shown on cards and in SEO/OG metadata.]'
status: active
startDate: 2026-01-01
technologies: ['Add', 'Your', 'Stack', 'Here']
technologyNotes:
  - name: 'Add'
    context: '[One sentence: what role does this play, or which phase/decision
      is it tied to? Shows in the Tech stack section — every name here should
      match an entry in `technologies` above.]'
githubUrl: 'https://github.com/your-username/project-name'
phases:
  - id: 'phase-1-slug'
    status: 'done'
    summary: '[One or two sentences: what this phase actually shipped or
      concluded — this shows in the phase carousel card, so make it
      substantive, not just a label.]'
  - id: 'phase-2-slug'
    status: 'in-progress'
    summary: '[What are you currently exploring in this phase, and what have
      you found so far?]'
  - id: 'phase-3-slug'
    status: 'planned'
    summary: "[Not started yet — a sentence on the plan is enough. It'll show
      as \"Planned\" on the carousel until you flip its status once you
      start.]"
relatedNotesSlugs: ['example-note']
order: 10
progress: 50
---

import Callout from '../../components/content/Callout.astro';
import PhaseJournal from '../../components/content/PhaseJournal.astro';
import PhaseCarousel from '../../components/content/PhaseCarousel.astro';
import TechStack from '../../components/content/TechStack.astro';

<Callout variant="info" title="Template — replace before publishing">
  This file demonstrates the `projects` collection schema
  (`src/content.config.ts`). Duplicate it for each real project, fill in
  the frontmatter above, and replace this body with real content. Delete
  this callout once you have.
</Callout>

## Overview

[2-4 sentences: what this project is and why it exists.]

## Problem

[What was broken, missing, or worth exploring?]

## Phases

[A short lead-in sentence — what's the arc across phases (e.g. "N phases so
far, from X to Y:"). Then a `###` subsection per phase, with the anchor id
given as `|#id|` at the end of the heading text (not `{#id}` — MDX treats `{`
as the start of a JS expression, so that syntax doesn't parse here). The id
must match a `phases[].id` entry above — that's how the phase carousel and
journal linking find it, and `phases[].summary` is what actually shows on
that phase's carousel card, so write it as real content (what happened /
what's the finding), not a label. Add journal entries for a phase by setting
`projectPhaseId: '[matching id]'` in the journal entry's own frontmatter;
nothing needs to change here when you do. Wrap the phase subsections in
`<PhaseCarousel>` — it groups everything from one `###` heading up to the
next into a slide, with prev/next controls, and falls back to plain
scrollable content if JS is disabled. Pass `phases={frontmatter.phases}` so
the carousel can color its jump dots by status.]

<PhaseCarousel phases={frontmatter.phases}>

### [Phase 1 name] |#phase-1-slug|

<PhaseJournal projectSlug="[this file's slug]" phaseId="phase-1-slug" />

[What did you build in this phase? What did it achieve?]

<Callout variant="lesson" title="[Name a lesson from this phase, if any]">
  [What did you learn, and what would you tell yourself at the start of
  this phase?]
</Callout>

### [Phase 2 name] |#phase-2-slug|

<PhaseJournal projectSlug="[this file's slug]" phaseId="phase-2-slug" />

[What's this phase about? What have you found so far?]

### [Phase 3 name] |#phase-3-slug|

<PhaseJournal projectSlug="[this file's slug]" phaseId="phase-3-slug" />

[Not started yet — a one-line description of the plan is enough. It'll show
as "Planned" on the stepper until you flip its status once you start.]

</PhaseCarousel>

## Architecture

[Describe the system's components and how they interact. If you have a
diagram (Excalidraw, Mermaid Live Editor export, etc.), save it under
`src/assets/` and embed it here with:]

```mdx
import { Image } from 'astro:assets';
import diagram from '../../assets/your-diagram.svg';

<Image src={diagram} alt="Describe the diagram for accessibility" />
```

## Tech stack

[One lead-in sentence, then the list below. Keep each `context` short —
this is a pointer to where the full reasoning lives (a phase or a decision
Callout), not a repeat of it.]

<TechStack notes={frontmatter.technologyNotes} />

## General decisions

[Standing, project-wide decisions that aren't tied to one phase — library
choices, scope cuts, deviations from a spec. Phase-specific decisions belong
next to the phase they were made in, under Phases above.]

<Callout variant="decision" title="[Name the decision]">
  [What choice did you make, what were the alternatives, and why did you
  pick this one? One callout per notable decision.]
</Callout>

## Approach

[A closing reflection on methodology/discipline — what worked, what you'd
tell yourself at the start, tying back to the phases and lessons above. Not
a repeat of Overview (what/why) — this is about how the work was actually
carried out.]

## Future improvements

[Open-ended, not-yet-scoped ideas. Once something is concrete enough to have
a "step one," give it its own phase above instead.]
