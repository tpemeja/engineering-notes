---
title: 'Belote AI'
description: 'Game-playing agents for Belote on OpenSpiel, built as a ladder of measured rungs: hand-built rules, Monte Carlo search, networks that replace the search, and a language model at the table.'
status: paused
startDate: 2026-07-24
technologies:
  [
    'Python',
    'OpenSpiel',
    'PyTorch',
    'LangGraph',
    'Ollama',
  ]
technologyNotes:
  - name: 'Python'
    context: 'The agents, the training, the measurement layer and both front ends.'
  - name: 'OpenSpiel'
    context: "Belote is python_belote in a fork of OpenSpiel, with a C++ twin verified identical over 40 seeded deals. The game's resampler is what every search agent stands on."
  - name: 'PyTorch'
    context: 'PlayNet (145 → 256 → 128 → 1) and BidNet (81 → 128 → 64 → 1), trained on Monte Carlo labels with train/validation splits grouped by deal.'
  - name: 'LangGraph'
    context: 'The four-step pipeline (facts computed in code, then infer, evaluate, choose) that tests whether decomposing a decision helps the language model.'
  - name: 'Ollama'
    context: 'Serves gemma4:12b locally for the language-model rungs; the MLX build runs 1.6x faster than GGUF for the same weights.'
  - name: 'pytest'
    context: "Statistics, the game-contract checks and the front ends, alongside the fork's own 42 rule tests."
  - name: 'uv'
    context: 'Dependency management, with the OpenSpiel fork installed as an editable path dependency.'
githubUrl: 'https://github.com/tpemeja/belote-ai'
highlights:
  - headline: '+38.7 pts/deal'
    detail: 'Monte Carlo policy improvement over the best hand-written rule, the largest gain on the ladder.'
  - headline: '148x faster'
    detail: 'A search-free network agent that ties Monte Carlo search over a 6,000-deal match.'
  - headline: 'Fix merged upstream'
    detail: "Found and fixed a seeding bug in OpenSpiel's Python ISMCTS."
    href: 'https://github.com/google-deepmind/open_spiel/pull/1584'
  - headline: 'Every claim measured'
    detail: 'Paired, dealer-swapped matches with confidence intervals, sized in advance to detect 5 points a deal.'
relatedNotesSlugs: []
order: 10
---

import Callout from '../../components/content/Callout.astro';
import PhaseLedger from '../../components/content/PhaseLedger.astro';
import Phase from '../../components/content/Phase.astro';
import TechStack from '../../components/content/TechStack.astro';
import Screenshot from '../../components/content/Screenshot.astro';
import DecisionTable from '../../components/content/DecisionTable.astro';
import BranchDiagram from '../../components/content/BranchDiagram.astro';
import NetworkDiagram from '../../components/content/NetworkDiagram.astro';
import ConfidenceIntervalChart from '../../components/content/ConfidenceIntervalChart.astro';

## Overview

<Screenshot
  src="/images/projects/belote-ai/live.webp"
  alt="Browser front end mid-deal: the current trick, the player's six-card hand, and the agent's evaluation of each legal card as a bar chart, with J of hearts best at +32.2 points per deal"
  caption="Playing the strongest agent in a browser. Before you choose, the Monte Carlo agent shows what each of your legal cards is worth, in points per deal."
/>

Game-playing agents for Belote, a four-player French trick-taking card game,
built on [OpenSpiel](https://github.com/google-deepmind/open_spiel). The
project is a ladder: each rung asks one question, answers it with a measured
comparison, and hands the result to the next, from fixed rules through Monte
Carlo search to neural networks and a local language model.

The strongest card play contains no network. One step of Monte Carlo policy
improvement over a hand-written rule is the largest gain on the ladder, and
the networks buy speed rather than strength: a search-free agent ties that
search at 148x the speed. Building the game's resampler also surfaced a
seeding bug in OpenSpiel's own ISMCTS, fixed upstream in
[open_spiel#1584](https://github.com/google-deepmind/open_spiel/pull/1584).

## Problem

- **Belote is not an OpenSpiel game.** There is no engine to build agents on,
  and "Belote" is many rule sets: this is classic *à la retourne*, with a
  take-or-pass auction, not Coinchée or Contrée.
- **One deal swings by roughly ±162 points, and not like a bell curve.** The
  all-or-nothing contract piles margins up at the extremes: one dealer
  orientation alone has a standard deviation of 123 points a deal, so "I won
  fifty deals" carries almost no information.
- **The hands are hidden.** A search agent has to reason over worlds
  consistent with what its seat actually knows, never over the real deal.
- **Bidding and card play confound each other.** A margin between two agents
  that differ in both halves can be attributed to neither, and a search agent
  that searches every action silently bids by search too.
- **A language model is too slow for a match.** At 7.7 to 16.7 seconds a
  decision, the thousands of deals a 5-point comparison needs are out of
  reach.

## Approach

Every claim of the form *A beats B* is a paired match: each deal is played
twice with the dealer swapped, the margin carries a 95% confidence interval,
and the deal count is solved in advance to detect 5 points a deal at 80%
power. An interval containing zero is reported as a tie, which is a bound
(smaller than about five points), not a claim of equality. A comparison meant
to isolate card play pins bidding, and a guard verifies it rather than
trusting the configuration.

Three instruments carry that rule to players of very different cost:
**matches** for whole agents, **decision grading** for a player too slow to
play hundreds of deals, and a **judge** that screens a card-play change too
small for a match, as a paired difference at the same stored positions. Every
rung writes its configuration, rows and findings to `results/ladder/`, so
each number on this page traces to a record in the repo.

## What I built

The agents the ladder converged on:

| Agent | Role | Result | Cost |
|---|---|---|---|
| `BidNet` + Monte Carlo | Strongest agent | +5.50 over Monte Carlo with rule bidding | ~120 ms/deal |
| `BidNet` + `PlayNet` | Fastest strong agent | Level with Monte Carlo, -0.63 [-2.06, +0.81] | ~0.9 ms/deal |
| Monte Carlo, 32 worlds | Strongest card play | +38.68 over the best rule, `win_or_dump` | ~120 ms/deal |
| `BidNet` | Bidding | +5.59 over the best rule, `hand_strength` 62+ | |

<PhaseLedger>

<Phase
  id="game-implementation"
  title="The game"
  status="done"
  goal="Implement Belote as an OpenSpiel game whose rules, scoring and resampling behave exactly as the agents built on it assume."
  outcome="Belote as an OpenSpiel game, its scoring and resampler checked against the public API."
>

`python_belote` lives in a fork of OpenSpiel: chance nodes deal the cards,
the framework enforces imperfect information, and one flat action space of
38 actions covers the auction and card play. The fork carries 42 rule tests;
the ladder's own first rung checks the contract with the agents instead:
returns zero-sum over 200 deals, trick points exactly 162 or 252, and
resampled worlds that keep the actor's hand, never resurrect a played card
and respect every inferred void.

<DecisionTable
  decisions={[
    {
      label: 'Unit of play',
      chose: 'One deal is one game, returning the signed point margin',
      insteadOf: 'A race to 501 or 1000 across deals',
      why: 'Every result becomes a mean of independent per-deal margins, and the game is zero-sum by construction.',
    },
    {
      label: 'Declarations',
      chose: 'Belote-rebelote only, granted automatically',
      insteadOf: 'Announcement actions, plus tierce, cinquante, cent and carré',
      why: 'Keeps the deck worth a fixed 162 (252 on a capot), which every confidence interval depends on, without adding a bluffing action.',
    },
    {
      label: 'Dealer',
      chose: 'A game parameter that rotates on each redeal',
      insteadOf: 'A fixed first seat',
      why: 'It is what makes dealer-swapped paired matches possible, the most important measurement device in the project.',
    },
  ]}
/>

<Callout variant="lesson" title="True is not the same as learnable">
  With no announcement action, the engine records who holds belote before a
  card is played. That is bookkeeping, not public information: anything shown
  to a player reads `belote_announced` instead, and the rung checks the gap
  is real in all 14 deals that had a holder.
</Callout>

</Phase>

<Phase
  id="simulation-harness"
  title="Measurement"
  status="done"
  goal="Build the instrument before any agent worth pointing it at, and prove it on cases where the right answer is known."
  outcome="Pairing removes 28% more noise than averaging, and a true null reads as a tie."
>

Who deals decides who bids first, which is worth real points, so an unpaired
comparison partly measures seat luck. The fix borrows duplicate bridge's
trick:

<BranchDiagram
  source={{
    name: 'One seeded shuffle',
    detail: 'the identical cards are dealt both times',
  }}
  branchLabel="play it twice, dealer rotated one seat"
  branches={[
    { name: 'Dealer at seat 0', detail: "one team bids first: this orientation's margin" },
    { name: 'Dealer at seat 1', detail: 'the other team bids first: the mirror margin' },
  ]}
  mergeLabel="average the two margins"
  merge={{
    name: 'Paired margin',
    detail: 'seat-order advantage cancels out',
  }}
/>

The rung measures the instrument with bots too simple to have opinions:

| Check | Result |
|---|---|
| Spread, one dealer orientation | sd 123.0 |
| Spread expected from averaging two draws alone | sd 87.0 |
| Spread, dealer-swapped pair | **sd 63.0**, 28% tighter than averaging |
| Random vs random, a known null | -0.04 [-3.52, +3.45] over 1,199 deals: **tie** |
| Sized to detect 5 points a deal | 1,153 deals, achieved half-width 3.54 |
| Bidding guard, always-take vs always-pass | caught at deal 0 |

</Phase>

<Phase
  id="hand-built-rules"
  title="Hand-built rules"
  status="done"
  goal="Find the strongest player that can be written by hand, with bidding and card play tuned separately and in the right order."
  outcome="Rules beat random by +12.6 a deal, and counting trumps beats counting points by +4.7."
>

The first agent splits into two independent halves, a bid rule and a
card-play rule, so every later comparison can freeze one and vary the other.
That split paid off immediately: the whole agent beats random by +12.55, but
isolated, bidding carries +20.86 while playing the highest-value card
**loses to random card play** by 3.84.

Card play was ranked first, bidding pinned, against `highest_value`:

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[
    { label: 'win_or_dump', mean: 45.26, ciLower: 41.92, ciUpper: 48.60, highlight: true },
    { label: 'help_partner', mean: 40.98, ciLower: 37.66, ciUpper: 44.30 },
    { label: 'conserve_trump_on_lead', mean: 34.48, ciLower: 31.16, ciUpper: 37.81 },
    { label: 'lowest_value', mean: 17.54, ciLower: 14.00, ciUpper: 21.08 },
    { label: 'random card play', mean: 1.85, ciLower: -1.58, ciUpper: 5.29 },
  ]}
/>

Only then was the bid threshold tuned, against `win_or_dump` rather than
against random. A second bid rule, `hand_strength`, adds trump length,
honours and voids to raw card points, and beats the tuned points rule by
+4.73 [+1.28, +8.19].

<Callout variant="lesson" title="Screening is not settling">
  `win_or_dump` led `help_partner` by 4.3 points against the common
  reference, and head to head they tie, +1.10 [-2.29, +4.50]. Thresholds 50
  and 56 looked equal to 44 against the old threshold, and both lose to it
  directly. A ranking against a shared opponent orders a field; only a direct
  comparison settles it.
</Callout>

</Phase>

<Phase
  id="monte-carlo"
  title="Monte Carlo"
  status="done"
  goal="Test whether one step of policy improvement over the best rule beats that rule, with no network, no tree and no training."
  outcome="One step of policy improvement over the best rule, no training: +38.7 a deal."
>

At each real choice the agent samples complete deals consistent with what
its seat knows, plays every legal card out in each under `win_or_dump`, and
keeps the best average. Two details do most of the work:

- **Common random numbers.** Every candidate card is scored on the same
  worlds, the pairing trick moved inside the agent.
- **Forced decisions are skipped.** About a third of card-play decisions have
  one legal card.

Against `win_or_dump`, bidding pinned on both sides:

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[
    { label: '4 worlds (14.5 ms/deal)', mean: 26.03, ciLower: 22.52, ciUpper: 29.54 },
    { label: '8 worlds (28.9 ms/deal)', mean: 27.44, ciLower: 23.95, ciUpper: 30.94 },
    { label: '16 worlds (56.3 ms/deal)', mean: 33.44, ciLower: 29.91, ciUpper: 36.97 },
    { label: '32 worlds (114.2 ms/deal)', mean: 38.68, ciLower: 35.18, ciUpper: 42.19, highlight: true },
  ]}
/>

Four worlds already capture 67% of the 32-world margin, so most of the gain
is the method. But every smaller budget also loses to 32 head to head, so
32 worlds is a floor, not an optimum, at 385x the rule's cost.

</Phase>

<Phase
  id="play-net"
  title="PlayNet"
  status="done"
  goal="Amortize the Monte Carlo agent into a network fast enough to play against, and find out what limits the copy."
  outcome="86% of Monte Carlo's margin at 1/152 of the cost; no lever closes the last 5.17 points."
>

`MCPlayBot.evaluate()` already values every legal card, so 25,000 played
deals yield 846,086 labels for free. `PlayNet` predicts each card's advantage
over the average legal card, on a trump-relative encoding, with the split
grouped by deal because ~34 decisions share each deal's outcome.

<NetworkDiagram
  layers={[
    { label: 'Input', size: 145, detail: '132 for the position, 13 for the candidate card' },
    { label: 'Hidden 1', size: 256, detail: 'Linear, ReLU' },
    { label: 'Hidden 2', size: 128, detail: 'Linear, ReLU' },
    { label: 'Output', size: 1, detail: "the card's advantage, in points" },
  ]}
  caption="Chosen over 128 × 64 by held-out R² (+0.411 against +0.405). Capacity is not the limit."
/>

It beats `win_or_dump` by +33.81 against the search's +39.12, at 0.79 ms a
deal against 120, and loses to the search head to head, -5.17 [-6.59,
-3.74]. Every lever on that gap was measured:

| Lever | Result |
|---|---|
| 128 label worlds instead of 32 | +0.018 R² |
| Twice the data | +1.14 in play, tie |
| Who played each card | -0.76 in play, tie |
| A listwise ranking loss | -0.08 [-0.63, +0.46], tie |
| As the search's rollout policy | +1.48, tie, at 1.4x the cost |
| A value net as the search's leaf evaluator | -4.03 at depth 8, -23.29 at depth 0 |

<Callout variant="lesson" title="Better against a reference is not closer to the teacher">
  Five times the training data moved `PlayNet` from +28.58 to +33.81 against
  the rule, and left it exactly as far from the search it imitates: -7.96
  before, -8.38 after, intervals overlapping.
</Callout>

</Phase>

<Phase
  id="bid-net"
  title="BidNet and the search-free agent"
  status="done"
  goal="Replace the hand-written bid rule with a network, then assemble both networks into an agent with no search at run time."
  outcome="BidNet out-bids the rule by +5.59; with PlayNet it ties Monte Carlo at 148x the speed."
>

The auction has no teacher, so each bid's value is measured by playing it
out. Every legal bid is cloned and played to the end with `PlayNet`, in 24
re-dealt worlds consistent with the bidder's hand, on the same worlds for
every bid. Resampling cuts label noise within a trump-length bucket from
147.2 to 46.5, and the labels read the way a player would expect:

| Round-1 trump length | Take minus pass |
|---|---|
| 0-2 | -54.9 |
| 3 | +28.1 |
| 4+ | +77.2 |

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[
    { label: 'BidNet vs rule, PlayNet playing (4,695 deals)', mean: 5.59, ciLower: 4.18, ciUpper: 6.99, highlight: true },
    { label: 'BidNet vs rule, Monte Carlo playing (6,000 deals)', mean: 5.50, ciLower: 4.05, ciUpper: 6.96 },
    { label: 'BidNet + PlayNet vs Monte Carlo agent (6,000 deals)', mean: -0.63, ciLower: -2.06, ciUpper: 0.81 },
  ]}
/>

`BidNet` is also less selective than the rule (0.20 redeals a deal against
0.55), so its margin is not bought by declining to play.

<Callout variant="lesson" title="Two runs, two verdicts, one effect">
  The same matchup read +6.75, a win, over 800 deals, and +1.92, a tie, at
  another seed. A run sized for 5 points cannot reliably see a 5-point
  effect. Resizing for 2 points settled it at +5.59, and turned a ±5 tie with
  the search into level within ±2.
</Callout>

</Phase>

<Phase
  id="decision-grading"
  title="Decision grading"
  status="done"
  goal="Measure a player one decision at a time, so that a player too slow or too costly for a match lands on a scale that already means something."
  outcome="One decision becomes one measurement: 411 positions rank every player in match order."
>

Monte Carlo at 128 worlds values every legal card at a position, so a choice
is scored exactly: **cost = best card's value - chosen card's value**. 411
decisions from 40 deals, each stored with the action history that rebuilds
the state, grade every player already on the ladder:

| Player | Points given up per decision | Picks a best card |
|---|---|---|
| Random legal card | 8.25 | 42.6% |
| `win_or_dump` | 4.01 | 61.6% |
| `PlayNet` | 2.69 | 71.3% |
| Monte Carlo, 32 worlds | 0.92 | 83.2% |
| The teacher, 128 worlds | 0.48 | 86.1% |

A player can also state claims, checked at three levels: public ("my partner
is winning"), hidden ("an opponent holds the ace") and future ("this ace
takes the trick"). The checker is tested against the hands it must not look
at, and agrees on all 120 positions.

<Callout variant="lesson" title="A grader ranks players; it does not predict margins">
  The language model gave up 10.80 points per decision over 10.7 decisions a
  deal, which suggests a 115.7-point loss. The match measured 35.92. A card is
  charged for a continuation that never happens, and a lost deal cannot be
  lost twice, so a winner still has to be confirmed in a match.
</Callout>

</Phase>

<Phase
  id="language-model"
  title="A language model at the table"
  status="done"
  goal="Put a local language model on the same scale as every other player: first graded one decision at a time, then over whole deals."
  outcome="A local 12B model gives up more per decision than a random card, and loses by 35.9 a deal."
>

`gemma4:12b`, served by Ollama, sees only what its seat can see and answers
with a card, a reason and any claims, under a schema that deliberately does
not enumerate the legal cards. On 150 graded decisions it gives up **9.28
points per decision** against 6.13 for a random legal card, and thinking
first does not help.

A LangGraph pipeline then hands it the counts it got wrong (trumps out,
master cards, what beats the trick) and splits the decision into four steps:

| | One call | Four steps |
|---|---|---|
| Points given up per decision | 9.15 | 9.23 |
| "My side wins this trick", correct | 62.1% | **78.7%** |
| Model calls / seconds per decision | 1 / 7.7 | 3 / 24.2 |

It describes the position better and chooses no better: what limits it is
judgement, not information. Over 24 whole deals against Monte Carlo it loses
**-35.92 [-52.54, -19.29]** a deal.

<Callout variant="lesson" title="It wins the trick in front of it and pays later">
  Its costliest cards come with fluent reasons: "leading a high trump to
  maintain control" gave up 116.6 points. The pattern holds across deals: it
  spends winners on tricks it did not need and calls it control.
</Callout>

</Phase>

<Phase
  id="play-front-end"
  title="Playing the agent"
  status="done"
  goal="Put a person at the table against the strongest agent, the only opponent from outside the project that any agent here faces."
  outcome="A person plays BidNet + Monte Carlo in a browser, then reviews and replays every decision."
>

The browser page is a view over the real `python_belote` state served from
Python, not a JavaScript copy of the rules, so a deal played there is the
same game the matches measure. A terminal version plays the same agent, and
both keep a running record of the human's margin, with its interval.

<Screenshot
  src="/images/projects/belote-ai/review.webp"
  alt="Post-deal review showing the contract verdict, all four dealt hands, and an eight-trick grid of who played which card, with trick winners outlined"
  caption="After the deal: the verdict, every hand as dealt, and every trick. The deal can be saved to a file and reopened later."
/>

<Screenshot
  src="/images/projects/belote-ai/replay.webp"
  alt="Card-by-card replay at trick 3, showing each seat's remaining hand and the evaluation of the J of hearts decision, identical to the live evaluation"
  caption="The replay steps through the deal a card at a time, with each seat's remaining hand and the evaluation made at that moment: the same +32.2 shown live, not recomputed."
/>

</Phase>

</PhaseLedger>

## Tech stack

What each piece of the stack is actually doing, and where to look for more:

<TechStack notes={frontmatter.technologyNotes} />

## Future improvements

The game, the hand-built player and the networks are settled; the language
model is paused. These are the known gaps, and the repo's
[`docs`](https://github.com/tpemeja/belote-ai/tree/main/docs) list each part's
measured dead ends alongside them:

- **Worlds sampled from a model of the hidden hands.** Monte Carlo draws the
  opponents' hands uniformly from every deal consistent with the play and
  ignores what the auction and discards say. A network predicting who holds
  which card would make a stronger teacher without changing anything
  downstream; Monte Carlo that sees the real deal bounds what it can buy, and
  is not yet measured.
- **Larger language models, and a harness aimed at the choice.** Everything
  measured is one local 12B model. Since handing it more facts did not help,
  the next harness should target the decision itself, for example the model
  proposing candidate cards for Monte Carlo to check. Bidding by the model is
  untested too.
- **Partner signalling.** No agent here signals on purpose, so tactical plays
  like an *appel* (calling for a suit through a discard) or an *impasse* (a
  finesse) are neither produced nor measured.
