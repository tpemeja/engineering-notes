---
title: 'Belote AI'
description: 'Game AI project: implementing Belote as an OpenSpiel game and building search- and learning-based agents to play it well.'
status: active
startDate: 2026-07-24
technologies:
  [
    'Python',
    'OpenSpiel',
    'PyTorch',
    'ISMCTS',
    'OpenSkill',
  ]
technologyNotes:
  - name: 'Python'
    context: 'The language OpenSpiel and the whole evaluation harness are
      written in.'
  - name: 'OpenSpiel'
    context: 'The game-engine framework Belote is implemented as a game
      inside of, instead of a bespoke engine (see General decisions).'
  - name: 'PyTorch'
    context: 'Backs the learned value network from the Guided rollout
      phase, which is still losing to RandomISMCTS.'
  - name: 'ISMCTS'
    context: "OpenSpiel's information-set MCTS algorithm, the strongest
      agent family in the project so far."
  - name: 'OpenSkill'
    context: 'Powers round-robin pool ratings for multi-agent comparison,
      chosen over TrueSkill (see General decisions).'
githubUrl: 'https://github.com/tpemeja/belote-ai'
phases:
  - id: 'game-implementation'
    status: 'done'
    summary: 'Create in OpenSpiel a correct, seedable Belote engine.'
  - id: 'simulation-harness'
    status: 'done'
    summary: 'Random-vs-random walking skeleton proving the harness itself:
      dealer-mirrored matches, paired win/tie/loss stats, and a pilot-then-size
      sample-size calculation instead of a guessed deal count. Result: -1.15
      pts/deal, 95% CI [-4.47, +2.17] over 1,393 paired deals, statistically
      indistinguishable from zero, as it should be for two identical bots.'
  - id: 'rulevalidbot'
    status: 'done'
    summary: 'Fixed bidding/play heuristics as the first real agent, beating
      the random baseline by +15.93 pts/deal, 95% CI [+12.19, +19.67], on
      1,250 deals sized by the walking skeleton pilot-then-size procedure.
      A take-threshold sweep then showed a stricter bidder does even better
      against random, up to a point.'
  - id: 'card-play-strategy'
    status: 'done'
    summary: 'A partner-aware, win-cheaply-or-discard-low card-play strategy,
      beating the highest-value baseline by +50.75 pts/deal, 95% CI [+47.37,
      +54.13]. A 5-agent OpenSkill tournament confirmed a bidding threshold
      tuned against random does not automatically transfer to a different
      opponent.'
  - id: 'ismcts'
    status: 'done'
    summary: "OpenSpiel's information-set MCTS, with a from-scratch
      resampler to keep sampled worlds consistent with what's actually been
      seen so far. Against the actual tournament champion,
      lead_conserve_trump, it's a decisive -17.35 pts/deal loss. A follow-up
      sweep of the simulation budget (10 to 400) never closes that gap,
      pointing at evaluator quality rather than search budget as the real
      bottleneck."
  - id: 'guided-rollout-value-function'
    status: 'in-progress'
    summary: 'Testing whether a heuristic or learned rollout policy beats
      ISMCTS default rollouts. Both are currently statistically
      indistinguishable from it.'
relatedNotesSlugs: []
order: 10
progress: 45
---

import Callout from '../../components/content/Callout.astro';
import PhaseJournal from '../../components/content/PhaseJournal.astro';
import PhaseCarousel from '../../components/content/PhaseCarousel.astro';
import TechStack from '../../components/content/TechStack.astro';
import ObjectDiagram from '../../components/content/ObjectDiagram.astro';
import DecisionTable from '../../components/content/DecisionTable.astro';
import ConfidenceIntervalChart from '../../components/content/ConfidenceIntervalChart.astro';
import BranchDiagram from '../../components/content/BranchDiagram.astro';
import WinTieLossBar from '../../components/content/WinTieLossBar.astro';
import PercentBars from '../../components/content/PercentBars.astro';
import SweepChart from '../../components/content/SweepChart.astro';
import {
  sweepMarginSeries,
  sweepWinRateSeries,
  sweepBreakdownSeries,
} from '../../data/belote-rulevalidbot-sweep.ts';

## Overview

The French trick-taking card game Belote is implemented as
a proper [OpenSpiel](https://github.com/google-deepmind/open_spiel) game
(chance-based dealing, imperfect information enforced by the framework, a
flat action space covering bidding and card play). 

Every agent built on top of it, from uniform random up to a PyTorch value 
network are compared against the others through a seeded, confidence-interval-backed 
evaluation harness rather than a handful of "it felt stronger" test games. 

A longer-term goal is bringing LLMs into that same comparison, not just to see
how they rank against other agents, but whether they can actually pull off Belote's 
tactical plays, like an *appel* (calling for a suit via a discard signal) or an 
*impasse* (a betting on where a specific card sits).

## Problem

Belote didn't exist as an OpenSpiel game, but the harder problem was proving
that any agent built for it was actually better than the last one :

- **No existing implementation.** Belote has no OpenSpiel game to build or
  benchmark agents against in the first place.
- **Evaluation noise compounds with imperfect information.** Small-sample
  results can look like real improvements until the sample size grows, and
  Belote's chance-based dealing and hidden hands push variance up further.
  So a *trustworthy* comparison needs enough seeded deals to be genuinely
  expensive to run.
- **Win rate alone won't reveal *how* an LLM plays.** Comparing an LLM against
  the other agents isn't just about who wins, it's whether the LLM can
  execute known tactical plays like an *appel* or an *impasse* at all, which
  an aggregate points margin can't show. That needs per-decision scoring
  against known-good tactical baselines, and LLM calls are too slow and
  expensive to lean on thousands of simulated deals per comparison the way
  ISMCTS or a value network can.

Any one of these is manageable alone, but together they mean win rate over a 
batch of games isn't enough. It needs a real implementation to run games on, 
enough seeded samples to separate skill from noise, and a way to score 
individual decisions for agents (such as an LLM) or human players, 
who can't realistically be evaluated over thousands of deals. 
That's why the evaluation harness became the first deliverable rather than 
an afterthought bolted on once agents existed.

## Phases

Six phases so far, from a uniform-random baseline to a learned value network:

<PhaseCarousel phases={frontmatter.phases}>

### Game Implementation |#game-implementation|

Belote is not part of the supported games in `OpenSpiel`. So the first part of the project is to
fork OpenSpiel and add a Belote implementation to it, well-tested, optimized, and documented
enough to eventually open a pull request back upstream. As I am more confident in Python, I
start there, planning to convert it to C++ later.

---

To implement the game in `OpenSpiel`, two objects need to be defined: 
- `Game`, which is relatively simple and only contains the parameters of the game.
- `State`, which contains all the game logic, including how the game is organized, the rules, and the scoring.

<ObjectDiagram
  arrowLabel="constructs"
  nodes={[
    {
      name: 'Game',
      fields: [
        { name: 'dealer', detail: 'ID of the player who deals' },
        { name: 'max_redeals', detail: 'cap on redeals if everyone passes' },
      ],
    },
    {
      name: 'State',
      fields: [
        {
          name: 'game logic',
          detail: 'bidding rounds, redeals, turn order, trick resolution',
        },
        { name: 'rules', detail: 'official FFBelote rules, incl. Belote/Rebelote' },
        {
          name: 'scoring',
          detail: 'capots, dedans, belote bonus, centered at 0 for RL',
        },
        {
          name: 'resampler',
          detail:
            "clones opponent hands, resampled but consistent with what's been observed",
        },
      ],
    },
  ]}
/>

---

Rules and dealing follow the [official FFBelote rules](https://www.ffbelote.org/wp-content/uploads/2016/01/regles-officielles-de-la-Belote-27-01-2016.pdf). OpenSpiel already supports non-deterministic games via chance nodes/players, and here that node is only in charge of dealing the cards.
A few things still needed a deliberate call:

<DecisionTable
  decisions={[
    {
      label: 'Game scope',
      chose: 'Single deal',
      insteadOf: 'Full game to a target score (e.g. 501)',
      why: "Lower variance and simpler confidence intervals at the cost of not directly measuring game-winning strategy (e.g. playing conservatively while ahead).",
    },
    {
      label: 'Rule fidelity',
      chose: 'Official rules, incl. auto-declared Belote/Rebelote',
      insteadOf: 'Relying on player declarations',
      why: 'Simplicity: the engine declares it for the player instead of trusting a signal.',
    },
    {
      label: 'RL scoring',
      chose: 'score = own_team_points − opponent_team_points',
      insteadOf: 'Raw official point totals (capots, dedans, belote bonus)',
      why: 'Centered-at-0 score matches the convention used by other OpenSpiel games like Bridge. Official points are still used for the game score itself.',
    },
  ]}
/>

---

The last important point for this section is how `resample_from_infostate` is defined, and its purpose: returning a clone of the other players' hands, resampled but kept consistent with the asking player's information state.
For a perfect-information game this is easy, since it's the same state, but for an imperfect-information game like Belote it's more complicated. Resampling started out naive and was extended once it under-used what a player could actually infer:

<ObjectDiagram
  arrowLabel="+ observed signals"
  nodes={[
    {
      name: 'Naive resample',
      fields: [
        {
          name: 'unplayed cards',
          detail: 'resampled uniformly at random among the other players',
        },
      ],
    },
    {
      name: 'Informed resample',
      fields: [
        {
          name: 'max trump card',
          detail: "inferred: a player who didn't over-trump holds no higher trump",
        },
        { name: 'voids', detail: 'suits a player is known not to hold' },
        {
          name: '2nd belote card',
          detail: 'known once Belote/Rebelote has been announced',
        },
      ],
    },
  ]}
/>

---

The game has been fully implemented in Python and tests have been created. I opened an issue in the official GitHub repository to ask some details for the pull request ([Add game "Belote"
#1577](https://github.com/google-deepmind/open_spiel/issues/1577)).
Before creating the PR, I will need to work on the C++ implementation for compute optimization. It will be a good opportunity to review C++ and compare the difference in performance.

### Simulation Harness |#simulation-harness|

<PhaseJournal projectSlug="belote-ai" phaseId="simulation-harness" />

Before writing any real agent, the harness itself needed proving: two
`pyspiel` uniform-random bots played against each other over seeded deals,
with the statistical machinery every later comparison depends on (paired
matches, confidence intervals, sample sizing) built and validated here
first instead of reinvented per phase.

---

A fixed dealer gives one team a permanent bidding-order advantage that would
otherwise be indistinguishable from a real skill difference. The fix borrows
duplicate bridge's trick: play the identical seed twice, dealer rotated one
seat, and average the two margins.

<BranchDiagram
  source={{
    name: 'Seed 42',
    detail: 'Same chance seed, so the identical cards get dealt both times.',
  }}
  branchLabel="play the same deal twice, dealer rotated one seat"
  branches={[
    { name: 'dealer = 0', detail: "Team 0 bids and plays first, this deal's normal margin." },
    { name: 'dealer = 1', detail: "Same deal, seats rotated one position: Team 1 bids and plays first instead, this deal's mirror margin." },
  ]}
  mergeLabel="average the two margins"
  merge={{
    name: 'Paired margin',
    detail: '(normal + mirror) / 2, seat-order bias cancels out.',
  }}
/>

<DecisionTable
  decisions={[
    {
      label: 'Match design',
      chose: 'Dealer-mirrored (paired) deals',
      insteadOf: 'A single fixed-dealer match',
      why: "Cancels the seat-order bias and roughly halves the margin's stdev on the same underlying deals, verified empirically, not just argued for.",
    },
    {
      label: 'Tie handling',
      chose: 'Report ties as their own bucket',
      insteadOf: 'Folding them into the win-rate denominator',
      why: 'An exact-zero paired margin means neither team came out ahead, a real outcome (~15% of deals here), not a rounding artifact to discard.',
    },
    {
      label: 'Sample size',
      chose: 'Pilot batch, then solve for N via a power calculation',
      insteadOf: 'A guessed deal count',
      why: 'A 300-deal pilot estimates variance, then a standard power formula solves for the N needed to detect a chosen minimum effect at a target confidence, checked against the achieved CI at the end.',
    },
  ]}
/>

For this random-vs-random run, the 300-deal pilot measured a paired stdev
of ~66.6 points/deal, which the power formula turned into a target of 1,393
paired deals (to detect a ±5 pt/deal effect at 80% power, 95% confidence).
Running that many gave a paired margin of **-1.15 pts/deal, 95% CI
[-4.47, +2.17]**, straddling zero, the result the harness needed to
produce before being trusted on anything else. Holding that same mean and
stdev fixed and only varying N shows why fewer deals wouldn't have said so
with any confidence, and why going far beyond 1,393 buys little more:

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[
    { label: 'N=50', mean: -1.15, ciLower: -18.66, ciUpper: 16.36 },
    { label: 'N=300 (pilot alone)', mean: -1.15, ciLower: -8.30, ciUpper: 6.00 },
    { label: 'N=1,393 (actual, sized)', mean: -1.15, ciLower: -4.47, ciUpper: 2.17, highlight: true },
    { label: 'N=5,000', mean: -1.15, ciLower: -2.90, ciUpper: 0.60 },
    { label: 'N=50,000', mean: -1.15, ciLower: -1.70, ciUpper: -0.60 },
  ]}
/>

Half-width shrinks with `1/sqrt(N)`, not linearly: 6x the deals (50→300)
only tightens it ~2.4x, and 36x more (1,393→50,000) tightens it just ~6x
further, the diminishing return the power calculation above is built
around. (Mean held fixed here purely to isolate N's effect. A real rerun
at a different N would land on a different mean too.)

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[
    { label: 'Dealer 0 only', mean: 13.58, ciLower: 7.07, ciUpper: 20.08 },
    { label: 'Dealer 1 only', mean: -15.87, ciLower: -22.44, ciUpper: -9.31 },
    {
      label: 'Paired (seat-order cancelled)',
      mean: -1.15,
      ciLower: -4.47,
      ciUpper: 2.17,
      highlight: true,
    },
  ]}
/>

The two single-dealer arms each look "significant" on their own (95% CIs
excluding zero), but in opposite directions and of similar size, exactly
the seat-order artifact the pairing exists to cancel, not a real skill gap
between identical bots. Only the paired arm (highlighted) straddles zero,
which is the correct answer.

<WinTieLossBar
  team0Label="Team 0 (Random)"
  team1Label="Team 1 (Random)"
  rows={[
    { label: 'Dealer 0 only', team0: 0.477, tie: 0.0, team1: 0.523 },
    { label: 'Dealer 1 only', team0: 0.518, tie: 0.0, team1: 0.482 },
    {
      label: 'Paired (seat-order cancelled)',
      team0: 0.404,
      tie: 0.153,
      team1: 0.443,
      highlight: true,
    },
  ]}
/>

Both single-dealer rows show 0% ties because one deal can never land on an
exact 0 margin. A made contract splits the points as actually won (say 90
vs 72). A failed one sweeps everything to the defenders instead. Either way
it's never perfectly even, since an 81-81 result is scored as a failed
contract rather than a tie.

The paired row's 15.3% is a different kind of tie: not one deal ending
even, but two mirrored deals landing on equal and opposite margins (Team 0
wins by X with dealer=0, Team 1 wins by that same X with dealer=1), which
average out to exactly 0. It's reported as its own outcome rather than
folded into the win-rate denominator, since it genuinely means neither team
had a net edge across both plays.

---

<Callout variant="lesson" title="A measurement tool has to prove it reports zero first">
  Two identical uniform-random bots should show a paired margin
  statistically indistinguishable from zero. That's not a throwaway smoke
  test, it's the calibration check the whole evaluation harness has to pass
  before any later "this agent is stronger" claim is allowed to rest on it.
</Callout>

### RuleValidBot |#rulevalidbot|

<PhaseJournal projectSlug="belote-ai" phaseId="rulevalidbot" />

The first real agent, deliberately minimal: the goal was reliably beating
the random baseline with a tight confidence interval, not playing well.

<ObjectDiagram
  nodes={[
    {
      name: 'RuleValidBot',
      fields: [
        {
          name: 'bid1 / bid2',
          detail: "take, or name the strongest suit, if the hand's trump-adjusted points are >= 31, otherwise pass",
        },
        { name: 'play', detail: 'play the highest point-value legal card' },
      ],
    },
  ]}
/>

Against uniform random, N sized by the same pilot-then-size procedure the
walking skeleton built (a 300-deal pilot measured stdev, which solved for
N=1,250 paired deals to detect a ±5 pt/deal effect at 80% power):

<WinTieLossBar
  team0Label="RuleValidBot"
  team1Label="Random"
  rows={[{ label: 'RuleValidBot vs Random', team0: 0.571, tie: 0.067, team1: 0.362, highlight: true }]}
/>

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[{ label: 'RuleValidBot vs Random', mean: 15.93, ciLower: 12.19, ciUpper: 19.67, highlight: true }]}
/>

Raw win rate and mean margin can disagree without contradicting each other.
Belote's all-or-nothing contract makes a loss a fixed, capped swing (fail
the contract and the other team sweeps 162), while a win's size is
uncapped, so a policy whose wins are bigger than its losses can win on
average while still losing more individual deals than it wins.

<Callout variant="info">
  Not charted above since only the paired result is shown, but the
  dealer=0-only arm behind this same result is exactly that case:
  RuleValidBot loses that raw count 45.1% to 54.9%, yet still averages
  +21.48 pts/deal on it.
</Callout>

Splitting *how often* a team gets to declare from *how well it does when it
does* told a more precise story than the headline number alone:

<PercentBars
  title="Declare frequency"
  bars={[
    { label: 'RuleValidBot declares', value: 0.303 },
    { label: 'Random declares', value: 0.697, highlight: true },
  ]}
/>
<PercentBars
  title="Declare success rate"
  bars={[
    { label: 'RuleValidBot succeeds', value: 0.708, highlight: true },
    { label: 'Random succeeds', value: 0.630 },
  ]}
/>

Random gets to attempt a contract more than twice as often (its bidding is
a blind coin flip that regularly preempts RuleValidBot before it even gets
a turn), yet still succeeds less often when it does. Raw win rate would
have conflated those two effects. Reporting them separately keeps the real
skill signal (70.8% vs 63.0% success) from hiding behind the opportunity
imbalance (30.3% vs 69.7% frequency), and vice versa.

That's not the same as showing "be more selective" is the right strategy,
though. RuleValidBot has no way to know in advance whether a given hand
will actually succeed, it only estimates via the trump-adjusted points
heuristic, and being pickier mechanically raises the success rate just by
filtering for stronger hands, at the cost of giving up most opportunities
entirely. Whether a given threshold trades away too much frequency for
that higher per-attempt rate is exactly what the sweep below tests.

The `>= 31` take-threshold and "always play highest-value card" were both
unexamined choices, so the natural next question was whether 31 was even a
good number. A sweep from 30 to 80, evaluated against the same fixed random
reference at each point (5,000 paired deals per threshold), answered that
directly:

<SweepChart
  series={sweepMarginSeries}
  xDomain={[29, 81]}
  yDomain={[0, 35]}
  xTicks={[30, 40, 50, 60, 70, 80]}
  yTicks={[0, 5, 10, 15, 20, 25, 30, 35]}
  xLabel="hand_points_threshold"
  yLabel="Margin vs random (pts/deal)"
  referenceLine={0}
/>

<SweepChart
  series={sweepWinRateSeries}
  xDomain={[29, 81]}
  yDomain={[0.5, 0.72]}
  xTicks={[30, 40, 50, 60, 70, 80]}
  yTicks={[0.5, 0.55, 0.6, 0.65, 0.7]}
  yTickFormat={(v) => `${Math.round(v * 100)}%`}
  xLabel="hand_points_threshold"
  yLabel="Paired win rate vs random"
  referenceLine={0.5}
/>

Margin and win rate climb steeply from threshold 30 up to about 43, then
plateau from there on. Threshold 58 is the nominal best on both
metrics (+31.36 pts/deal, 69.9% win rate), but it isn't meaningfully
distinguishable from most of the 40s and 50s, every point's CI in the
plateau overlaps its neighbors. The real finding is "stop being reckless
past ~43," not "58 is the answer."

Splitting *how often* RuleValidBot declares from *how well it does when it
declares vs. defends* explains *why* the plateau happens, not just that it
does:

<SweepChart
  series={sweepBreakdownSeries}
  xDomain={[29, 81]}
  yDomain={[0, 1]}
  xTicks={[30, 40, 50, 60, 70, 80]}
  yTicks={[0, 0.2, 0.4, 0.6, 0.8, 1]}
  yTickFormat={(v) => `${Math.round(v * 100)}%`}
  xLabel="hand_points_threshold"
  yLabel="Rate"
  height={280}
/>

RuleValidBot's hand-points threshold has a real ceiling: a 5-card hand
can't score above roughly 66, so past that it never declares at all, and
its declare rate is already under 5% by the mid-40s. Its win rate *when it
does declare* keeps climbing as the threshold rises, past 90% at the high
end, though noisy there (the widening band) since only a handful of hands
ever qualify. Its win rate *when defending*, on the other hand, barely
moves: flat around 42% regardless of threshold, since that side depends
only on how often Random's own blind bid fails, not on anything
RuleValidBot decides.

<Callout variant="lesson" title="A paired win rate isn't the same statistic as the per-deal rate behind it">
  Past threshold ~67, RuleValidBot never declares, so every deal is decided
  purely by whether Random's contract succeeds, and win rate when defending
  sits at 42.2%. Naively, the overall paired win rate should land near
  there too. It doesn't, it's 69.6%, confirmed directly by rerunning 3,000
  deals at a threshold well past that ceiling and inspecting the raw
  margins. Cause: Belote's
  all-or-nothing contract makes wins and losses wildly asymmetric in size
  (a rare failed-random-contract win averages +166.6, a common
  successful-random-contract loss averages only -69.3), and averaging two
  draws from a distribution shaped like that pushes the paired split well
  past what the raw per-deal split alone would suggest. A paired win rate
  and a per-deal win rate are genuinely different statistics, not two views
  of the same number.
</Callout>

<Callout variant="lesson" title="A parameter tuned against one opponent doesn't automatically transfer">
  The sweep holds everything fixed and evaluates each threshold against a
  *fixed* reference (random), the same principle chess engines use for SPSA
  tuning against a fixed reference engine, deliberately not another
  RuleValidBot threshold, so the scale doesn't shift as candidates change.
  That's the right way to find "better against random," but a threshold
  climbing purely by capitalizing on random's careless contract failures
  isn't necessarily better against a less careless opponent later.
  Confirmed later, not just a hedge: the next phase's partner-aware
  card-play strategy found exactly that, a threshold tuned against random
  didn't automatically carry over once the opponent got smarter.
</Callout>

### Card-play Strategy |#card-play-strategy|

<PhaseJournal projectSlug="belote-ai" phaseId="card-play-strategy" />

RuleValidBot's card play was as naive as its bidding was deliberately
minimal: always play the highest point-value legal card, regardless of
whether the trick can even be won or who's already winning it. This phase
replaces that with a partner-aware strategy, `candidate`.

<ObjectDiagram
  nodes={[
    {
      name: 'candidate',
      fields: [
        { name: 'leading', detail: 'play the highest point-value card' },
        {
          name: 'partner winning',
          detail: 'play the lowest point-value card',
        },
        {
          name: 'can beat the winner',
          detail: 'play the cheapest legal card that wins',
        },
        {
          name: 'cannot beat it',
          detail: 'play the lowest point-value card',
        },
      ],
    },
  ]}
/>

The "partner winning" branch is easy to get backwards: it plays the
*lowest* card, not the highest. A trick's point total is the sum of all
four cards played to it, regardless of who wins it, so adding a high card
to a trick your partner already has locked up doesn't win you anything
extra, it just burns a strong card you'll want in a later trick, and risks
losing it outright if a later opponent still overtakes the trick before
it's done. Play cheap when the trick's already won, go all-in only when
you're the one who actually has to win it.

Against step 01's `highest_value` baseline, same bidding threshold (31) on
both sides, N sized by the same pilot-then-size procedure as every match
so far:

<WinTieLossBar
  team0Label="candidate"
  team1Label="highest_value"
  rows={[
    {
      label: 'candidate vs highest_value',
      team0: 0.779,
      tie: 0.158,
      team1: 0.063,
      highlight: true,
    },
  ]}
/>

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[
    {
      label: 'candidate vs highest_value',
      mean: 50.75,
      ciLower: 47.37,
      ciUpper: 54.13,
      highlight: true,
    },
  ]}
/>

Not a marginal improvement. Always playing highest value routinely beats
your own partner's already-winning card for no reason, and just as
routinely burns strong cards on tricks that are already lost, partner-aware
and trump-aware logic fixes both for free.

`candidate` beating `highest_value` confirms partner-awareness helps at
all, but it doesn't mean partner-awareness this specific way is the best
idea available. Four more strategies were added deliberately, each testing
one specific idea rather than just assuming it:

<DecisionTable
  decisions={[
    {
      label: 'lowest_value',
      chose: 'Always discard the cheapest legal card',
      insteadOf: "Assumed as a weak floor for comparison",
      why: "Turned out not weak at all: conserving strong cards for tricks actually worth winning beats highest_value's zero-awareness burn.",
    },
    {
      label: 'help_partner',
      chose: 'Play highest when partner is already winning',
      insteadOf: "candidate's play-lowest choice in that branch",
      why: 'A plausible "help them win big" instinct, added specifically to measure it rather than just argue about it, since a trick\'s points go to the winning team regardless of which card either teammate contributed.',
    },
    {
      label: 'lead_conserve_trump',
      chose: 'Prefer a non-trump card when leading',
      insteadOf: "candidate's highest-value-regardless lead",
      why: 'Avoids burning trump early for no reason, before there is even a trick to fight for yet.',
    },
    {
      label: 'second_hand_low',
      chose: 'Always play low at trick position 2/3, decide only when last to act',
      insteadOf: "candidate's partner-status check at every position",
      why: 'Isolates position as the deciding factor instead of partner status, the classic trick-taking heuristic.',
    },
  ]}
/>

All 6 strategies share the exact same bidding logic, only card play
differs, so before ranking them against each other properly, each first
needed its own bidding threshold determined against the fixed random
reference, the same sweep step 01 ran for `highest_value` alone, now
generalized across all 6:

| Strategy | Best threshold | Margin vs random | Win rate |
|---|---|---|---|
| `lead_conserve_trump` | 49 | +65.90 | 85.9% |
| `candidate` | 49 | +54.17 | 80.4% |
| `help_partner` | 58 | +51.95 | 79.5% |
| `second_hand_low` | 49 | +50.24 | 78.7% |
| `lowest_value` | 67 | +44.32 | 76.5% |
| `highest_value` | 58 | +31.36 | 69.9% |

The best threshold genuinely differs by strategy (49 to 67), even though
bidding logic is identical across all six. That's not noise, it's a real
interaction: a stronger card-play strategy can afford to declare on a
wider range of hands, since it's more likely to turn a borderline hand
into a made contract, while a weaker one needs a stricter filter to
compensate. Bid and play aren't independently tunable stages.

With every strategy at its own best threshold, a 7-agent round-robin
(random plus all 6, 5,000 paired deals per pairing) ranks them the same
way as the first tournament, one statistically-vetted verdict per pairing
into OpenSkill, not raw per-deal outcomes. OpenSkill tracks each agent as
a skill *distribution* (a mean `mu` and an uncertainty `sigma`), not a
single number, and the **ordinal** below (`mu - 3*sigma`) is its
conservative, deliberately pessimistic point estimate for ranking: an
agent's true skill is claimed to sit above its ordinal with high
confidence, so a wide gap between two agents' ordinals is a safer signal
than comparing their `mu`s directly:

<ConfidenceIntervalChart
  unit="ordinal"
  metrics={[
    { label: 'lead_conserve_trump@49', mean: 16.45, ciLower: 9.53, ciUpper: 23.37, highlight: true },
    { label: 'candidate@49', mean: 12.94, ciLower: 6.0, ciUpper: 19.89 },
    { label: 'second_hand_low@49', mean: 8.07, ciLower: 1.17, ciUpper: 14.97 },
    { label: 'help_partner@58', mean: 4.5, ciLower: -2.42, ciUpper: 11.42 },
    { label: 'highest_value@58', mean: 0.3, ciLower: -6.7, ciUpper: 7.3 },
    { label: 'lowest_value@67', mean: -2.69, ciLower: -9.72, ciUpper: 4.33 },
    { label: 'random', mean: -7.56, ciLower: -14.66, ciUpper: -0.47 },
  ]}
/>

`lead_conserve_trump` is the strongest strategy found so far, beating
`candidate` head-to-head by +14.60 pts/deal, trump conservation on lead
mattered more than the partner-aware follow logic that was this phase's
original headline result. `candidate` also beats `help_partner`
head-to-head by +30.75 pts/deal, confirming the "burning a good card for
nothing" hypothesis directly rather than resting on the argument for it.

<Callout variant="lesson" title="A whole strategy ranking can invert, not just a threshold">
  Against random alone, `lowest_value` (+44.32 pts/deal) clearly beat
  `highest_value` (+31.36). Head-to-head in this round-robin, it's the
  opposite: `highest_value@58` beats `lowest_value@67` by +10.30 pts/deal,
  95% CI [+9.39, +11.21], not noise. Same root cause as a threshold not
  transferring across opponents, a fixed reference only validates against
  that reference, but a stronger version of the claim: it's not just the
  best *setting* for a strategy that can fail to generalize, it's which of
  two whole strategies is even *better* that can flip depending on the
  opponent. A round-robin against several distinct opponents is the only
  way to know which of two things is actually better in general.
</Callout>

### ISMCTS |#ismcts|

<PhaseJournal projectSlug="belote-ai" phaseId="ismcts" />

OpenSpiel's information-set MCTS, which required implementing
`resample_from_infostate` on `BeloteState` from scratch. Uniform sampling
wasn't enough, it needed to respect suit-following/over-trump history to
produce *consistent* worlds. This general technique (resample a plausible
fully-observed world consistent with what the searching player actually
knows, run ordinary MCTS on it, repeat over many sampled worlds) is called
PIMC (Perfect Information Monte Carlo) or determinization search, the same
approach real bridge- and poker-playing programs use.

<Callout variant="lesson" title="Plain MCTSBot silently cheats on imperfect-information games">
  OpenSpiel's `MCTSBot` doesn't reject imperfect-information games outright.
  It runs against Belote without error. But `BeloteState` stores all four
  hands as plain visible attributes, so MCTS's tree search has full access to
  opponents' hidden cards during rollout. An omniscient bot in an evaluation
  pool produces misleading win-rate and Elo numbers for *every other* agent it
  plays against, not just itself. `ISMCTSBot`, which samples consistent worlds
  via `resample_from_infostate` instead of looking at the true state, is the
  only MCTS-family bot allowed in the project as a result.
</Callout>

---

With the resampler complete, ISMCTS was measured against step 02's actual
tournament champion, `lead_conserve_trump@49`:

<WinTieLossBar
  team0Label="ISMCTS"
  team1Label="lead_conserve_trump"
  rows={[{ label: 'ISMCTS vs lead_conserve_trump', team0: 0.379, tie: 0.007, team1: 0.614, highlight: true }]}
/>

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[{ label: 'ISMCTS vs lead_conserve_trump (N=1,550)', mean: -17.35, ciLower: -21.13, ciUpper: -13.56, highlight: true }]}
/>

A clear, decisive result. `lead_conserve_trump` is a genuinely stronger
opponent than the `candidate` baseline ISMCTS was originally tested
against, beating it by +14.60 pts/deal in its own right (step 02's
tournament), so this reflects a tougher matchup, not a regression.

---

At only 25 simulations with a pure-random rollout evaluator, ISMCTS wasn't
yet competitive with the best hand-crafted heuristic found so far, leaving
one open question: does more search budget close the gap? A sweep of
`max_simulations` over a 40x range, same fixed opponent
(`lead_conserve_trump@49`), 200 paired deals per point, a fixed and modest
count rather than the usual pilot-then-size procedure: sizing to a target
effect at each point would make the already-most-expensive
high-simulation points also demand the most deals:

<ConfidenceIntervalChart
  unit="pts/deal"
  metrics={[
    { label: '10 simulations', mean: -31.05, ciLower: -41.93, ciUpper: -20.16 },
    { label: '25 simulations', mean: -18.45, ciLower: -27.86, ciUpper: -9.03 },
    { label: '50 simulations', mean: -18.31, ciLower: -28.76, ciUpper: -7.85 },
    { label: '100 simulations', mean: -5.00, ciLower: -15.10, ciUpper: 5.10, highlight: true },
    { label: '200 simulations', mean: -11.89, ciLower: -22.81, ciUpper: -0.97 },
    { label: '400 simulations', mean: -19.24, ciLower: -29.49, ciUpper: -8.99 },
  ]}
/>

Cost scales as expected, think time per decision is roughly linear in
`max_simulations`:

| `max_simulations` | ms/decision |
|---|---|
| 10 | 2.8 |
| 25 | 6.6 |
| 50 | 13.9 |
| 100 | 30.4 |
| 200 | 67.9 |
| 400 | 159.2 |

A ~57x cost increase for a 40x simulation increase, close to proportional.

<Callout variant="lesson" title="A wiggly curve isn't automatically a signal">
  The margin improves steadily from 10 to 100 simulations, then gets *worse*
  again from 100 to 400, back down near the 25/50-simulation level. Read as a
  single line, that shape says "diminishing returns, then it gets worse," a
  specific structural claim. But each point is only 200 paired deals, and
  neighboring CIs substantially overlap (sims=100's upper bound and sims=400's
  lower bound touch between -15.10 and -8.99). The one thing that does hold
  up: sims=100 is the sole point whose CI includes zero, though with six
  points swept, one landing on the "includes zero" side by chance is expected
  even under a smooth true relationship, a reason to collect more data at
  that point specifically, not to declare a sweet spot.
</Callout>

The headline holds regardless of the wiggle: across the entire tested
range, a 40x compute increase from 10 to 400 simulations, ISMCTS never
clearly closes the gap with `lead_conserve_trump`. Even the best point is
only "not distinguishable from even," not "ahead." Spending more search
budget on a random-rollout evaluator doesn't reliably buy strength here,
evidence for why the next phase went after evaluator quality instead of
simulation count as the lever to pull.

### Guided rollout & learned value function |#guided-rollout-value-function|

<PhaseJournal projectSlug="belote-ai" phaseId="guided-rollout-value-function" />

Exploring whether a heuristic or learned rollout policy beats ISMCTS's
default random rollouts. Both are currently inconclusive (see
[Future improvements](#future-improvements) below).

<Callout variant="lesson" title="A promising small-sample result didn't survive more data">
  An epsilon-greedy guided rollout looked like a real improvement over ISMCTS's
  default rollout at N=1,000 simulated deals (+1.13 pts/deal), but regressed to
  a statistically-indistinguishable -0.27 at N=10,000, with confidence
  intervals overlapping zero. Random, deterministic, and epsilon=0.3 rollouts
  all turned out to be indistinguishable at the tested simulation budget, a
  concrete example of why the harness reports confidence intervals rather than
  point estimates, and why a result needs to survive a larger sample before
  it's trusted.
</Callout>

</PhaseCarousel>

## Architecture

The game engine (`BeloteGame`/`BeloteState`) lives in a personal OpenSpiel
fork rather than in `belote-ai` itself, a deliberate decision to build on
OpenSpiel's existing imperfect-information machinery and agent library
(uniform random, MCTS, ISMCTS, the CFR family) instead of re-implementing all
of that from scratch. `belote-ai` depends on the fork as an editable/path
install and owns only:

```
belote/
├── agents/
│   ├── registry.py         # name -> pyspiel.Bot factory
│   └── rule_valid_bot.py
├── eval/
│   ├── match.py            # evaluate_bots wrapper: duplicate deals, CIs, Elo
│   ├── decision.py         # decision-quality scoring
│   └── throughput.py       # games/sec benchmark
├── cli.py
└── tests/
```

Agents implement OpenSpiel's `Bot` contract directly (`step`, `restart_at`,
optional `inform_action`) rather than a bespoke interface, so legality is
enforced by `state.legal_actions()` and information hiding is enforced by
OpenSpiel itself. An agent only ever sees its own information state, never
the full game state, so it's structurally impossible for a correctly-written
bot to cheat.

## Tech stack

What each piece of the stack is actually doing, and where to look for more:

<TechStack notes={frontmatter.technologyNotes} />

## General decisions

Standing, project-wide choices that aren't tied to a single phase.

<Callout variant="decision" title="Build on OpenSpiel instead of a bespoke game engine">
  Belote is implemented as an OpenSpiel game rather than a standalone engine,
  inheriting its imperfect-information state model, a large library of
  ready-made agents (random, MCTS, ISMCTS, CFR), `evaluate_bots` for
  agent-vs-agent play, and built-in correctness tooling (`random_sim_test`,
  golden playthroughs). The trade-off: the game lives in a personal fork of
  OpenSpiel (`python_belote` isn't upstream yet), so belote-ai's
  reproducibility is currently tied to a pinned commit of that fork rather than
  a released package, until the game is upstreamed.
</Callout>

<Callout variant="decision" title="OpenSkill over TrueSkill for multi-agent ratings">
  OpenSkill was chosen for round-robin pool ratings over Microsoft's TrueSkill
  because it's MIT-licensed and patent-free, and it models team games (Belote's
  2v2 structure) natively rather than requiring a workaround.
</Callout>

<Callout variant="decision" title="Cap redeals instead of matching the official unlimited-redeal rule">
  If both bidding rounds end with everyone passing, official rules redeal
  indefinitely. The harness caps this at 10 attempts and treats an
  all-attempts-exhausted deal as a flat draw (all four `returns()` are 0)
  rather than an error, a deliberate, documented deviation from the official
  rules, made because every evaluation module needs a well-defined outcome for
  every deal, and an unbounded loop doesn't provide one.
</Callout>

## Approach

Every phase above was built and evaluated against the harness before moving
to the next, rather than assumed. That discipline paid for itself
repeatedly: it caught a plain-MCTSBot "cheat" (full hand visibility during
rollout), a hidden RNG silently breaking reproducibility, and a promising
epsilon-greedy result that looked real at 1,000 simulated deals but
collapsed to statistically indistinguishable at 10,000, each one caught
before it could be mistaken for real progress.

## Future improvements

The immediate open problem is the learned value network (Guided rollout &
learned value function phase above), which is still losing decisively to
RandomISMCTS and shows two tangled issues: likely distribution shift between
the self-play data it was trained on and the positions it's evaluated on, and
clear overfitting (test loss plateaus around epoch 35 while training
continues to 200 with no early stopping). Isolating those two independently
is the next step, before deciding whether the value function is a viable
replacement for ISMCTS's rollouts at all. Longer term, the goal is to
upstream `python_belote` into `google-deepmind/open_spiel` so the project no
longer depends on a personal fork for its core game implementation.

Separately, worth revisiting once there's time for it: Belote's redeal
("everyone passes twice") is currently handled as an in-tree chance
branch, which is what triggered the `allow_inconsistent_action_sets`
`KeyError` in the ISMCTS phase. OpenSpiel's own Euchre implementation
avoids that entire class of bug by rule design, `stick_the_dealer=true`
forces the dealer to name trump on the last bid so an all-pass redeal can
never happen at all. Belote's official rules don't allow that shortcut,
but terminating the state on any redeal and having the harness deal a
fresh one externally would remove the collision the same way, and would
arguably be more rule-faithful too, since a thrown-in deal doesn't score
under the official rules either. Not urgent since the existing flag fully
resolves the crash, but a cleaner model than looping in-tree if the game
implementation gets revisited.
